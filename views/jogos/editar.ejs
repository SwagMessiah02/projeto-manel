<!-- Arquivo: views/jogos/editar.ejs -->
<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8" />
    <title>Editar Jogo</title>
    <link rel="stylesheet" href="/css/styles.css" />
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Bootstrap JS (animações, collapse, etc) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" defer></script>

    <!-- Seu CSS -->
    <link rel="stylesheet" href="/css/styles.css">

    <link rel="icon" href="/img/steam_icon.ico"/>

</head>

<body>
    <header class="steam-header d-flex align-items-center">
    <img
        src="https://upload.wikimedia.org/wikipedia/commons/8/83/Steam_icon_logo.svg"
        alt="Steam Logo"
        class="steam-logo"
    />
    <span class="steam-title">Jogos Steam</span>
    </header>

    <h1>Editar Jogo</h1>
    <form action="/jogos/editar/<%= jogo.id %>" method="POST">
        <p><label><h5>Título:</h5></label><br /><input name="titulo" value="<%= jogo.titulo %>" required /></p>
        <p><label><h5>Plataforma:</h5></label><br /><input name="plataforma" value="<%= jogo.plataforma %>" required /></p>
        <p>
            <label><h5>Dono (Amigo):</h5></label><br />
            <select name="amigoId" required>
                <% amigos.forEach(a=> { %>
                    <option value="<%= a.id %>" <%=Number(a.id)===Number(jogo.amigoId) ? 'selected' : '' %>>
                        <%= a.nome %> (<%= a.email %>)
                    </option>
                    <% }) %>
            </select>
        </p>
        <p><button type="submit">Salvar</button> <a href="/jogos">Cancelar</a></p>
    </form>
</body>

</html>