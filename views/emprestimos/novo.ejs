<!-- Arquivo: views/emprestimos/novo.ejs -->
<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8" />
    <title>Novo Empréstimo</title>
    <link rel="stylesheet" href="/css/styles.css" />
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Bootstrap JS (animações, collapse, etc) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" defer></script>

    <!-- Seu CSS -->
    <link rel="stylesheet" href="/css/styles.css">

    <link rel="icon" href="/img/steam_icon.ico"/>

</head>

<body>

        <header class="steam-header d-flex align-items-center">
        <img
            src="https://upload.wikimedia.org/wikipedia/commons/8/83/Steam_icon_logo.svg"
            alt="Steam Logo"
            class="steam-logo"
        />
        <span class="steam-title">Empréstimos Steam</span>
        </header>


    <h1>Registrar Empréstimo</h1>
    <form action="/emprestimos/novo" method="POST">
        <p>
            <label><h5>Jogo:</h5></label><br />
            <select name="jogoId" required>
                <% jogos.forEach(j=> { %>
                    <option value="<%= j.id %>">
                        <%= j.titulo %> (<%= j.plataforma %>)
                    </option>
                    <% }) %>
            </select>
        </p>
        <p>
            <label><h5>Amigo que pegou:</h5></label><br />
            <select name="amigoId" required <% amigos.forEach(a=> { %>
                <option value="<%= a.id %>">
                    <%= a.nome %> (<%= a.email %>)
                </option>
                <% }) %>
            </select>
        </p>
        <p><label><h5>Data de início:</h5></label><br /><input name="dataInicio" placeholder="AAAA-MM-DD" required /></p>
        <p><label><h5>Data de fim (opcional):</h5></label><br /><input name="dataFim" placeholder="AAAA-MM-DD" /></p>
        <p><button type="submit">Salvar</button> <a href="/emprestimos">Cancelar</a></p>
    </form>
</body>

</html>